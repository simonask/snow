Object.instance_prototype.eval {
  .tap: [yield] { yield(self); self }
  // Suggestion: .tap! for .eval(yield)
  
  .getter(#ancestors) {
    result: @()
    current: .prototype
    while current
     result << current
     current: current.prototype
    end
    result
  }
}
