class: [closure_or_base, closure] {
	base: Object
	if closure
		base: closure_or_base
	else
		closure: closure_or_base
	end
	
	new: Class(base)
	new.instance_prototype.object_eval(closure)
	new.__reset_assigned_name__()
}